/**
 * Lucide icons - re-exported for the Surveil design system
 * All icons from https://lucide.dev/icons/
 *
 * Usage:
 *   import { Home, Settings } from '@/icons';
 *   import { Icon } from '@/components/Icon';
 *   <Icon icon={Home} size="md" />
 *
 * For dynamic icon loading by name, use IconByName from components/Icon
 */

export * from 'lucide-react';
